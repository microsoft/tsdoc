{
  "changes": [
    {
      "packageName": "@microsoft/tsdoc",
      "comment": "Fix round-trip emission of `@example` block titles so the title text is preserved on the tag line, and expose the tag-line title via new `DocBlock.title` and `DocBlock.body` accessors",
      "type": "minor"
    }
  ],
  "packageName": "@microsoft/tsdoc"
}
