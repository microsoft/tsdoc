# tsdoc-build-rig

A rig package for projects that build using Heft inside the TSDoc repository. This
package extends `@rushstack/heft-node-rig` and `@rushstack/heft-web-rig` and adds some options that are specific to the
TSDoc repository.

Note that this rig is not published to the NPM registry.